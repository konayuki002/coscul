<h1>New Production line</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.production_line_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.production_line_path(@conn, :index) %></span>
